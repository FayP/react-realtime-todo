import React from 'react';

const Toolbar = () => (
            <div>
                <input type="text" placeholder="Add an item" />
                <button>X</button>
            </div>
        )

export default Toolbar;